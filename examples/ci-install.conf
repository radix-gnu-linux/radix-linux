# Destructive on purpose. Only used with the disposable QEMU disk created by
# scripts/qemu-install-smoke.sh.
FIRMWARE=uefi
HOSTNAME=radix-ci
TIMEZONE=UTC
KEYMAP=us
LOCALE=C.UTF-8
DESKTOP=kde
LIBC=glibc
KERNEL=lts
GPU_DRIVER=mesa
FILESYSTEM=ext4
SWAP_GIB=0
MODE=erase
DISK=/dev/vda
ERASE_CONFIRM=/dev/vda
USERNAME=tester
ADMIN=yes
ONLINE_INSTALL=no
SERIAL_CONSOLE=yes
